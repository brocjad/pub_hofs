name: daily_hof

# Run this workflow every time a new commit pushed to your repository
on:
  workflow_dispatch

jobs:
  # Set the job key. The key is displayed as the job name
  # when a job name is not provided
  run_12:
    # Name the Job
    name: run_12
    # Set the type of machine to run on
    runs-on: ubuntu-18.04

    steps:
      # Checks out a copy of your repository on the ubuntu-latest machine
      - name: Checkout code
        uses: actions/checkout@v2

      - name: set up python 3.6
        uses: actions/setup-python@v2
        with:
          python-version: 3.6

      - name: install packages
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
          GOTAUTH: ${{ secrets.GOTAUTH}}
        run: |
          cd ../../
          pwd
          git clone https://$GOTAUTH@github.com/brokenjade3000/got_auto.git
          cd got_auto
          chmod 775 ./script/install.sh
          ./script/install.sh
          
      # Runs the Super-Linter action
      - name: run_12
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
        run: |
          pwd
          cd /home/runner/work/got_auto/src
          python --version
          python run_daily_hof.py 1,2,3,4


 

  run_56:
    # Name the Job
    name: run_56
    # Set the type of machine to run on
    runs-on: ubuntu-18.04

    steps:
      # Checks out a copy of your repository on the ubuntu-latest machine
      - name: Checkout code
        uses: actions/checkout@v2

      - name: set up python 3.6
        uses: actions/setup-python@v2
        with:
          python-version: 3.6

      - name: install packages
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
          GOTAUTH: ${{ secrets.GOTAUTH}}
        run: |
          cd ../../
          pwd
          git clone https://$GOTAUTH@github.com/brokenjade3000/got_auto.git
          cd got_auto
          chmod 775 ./script/install.sh
          ./script/install.sh
          
      # Runs the Super-Linter action
      - name: run_56
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
        run: |
          pwd
          cd /home/runner/work/got_auto/src
          python --version
          python run_daily_hof.py 5,6,7,8         

            
  run_910:
    # Name the Job
    name: run_910
    # Set the type of machine to run on
    runs-on: ubuntu-18.04

    steps:
      # Checks out a copy of your repository on the ubuntu-latest machine
      - name: Checkout code
        uses: actions/checkout@v2

      - name: set up python 3.6
        uses: actions/setup-python@v2
        with:
          python-version: 3.6

      - name: install packages
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
          GOTAUTH: ${{ secrets.GOTAUTH}}
        run: |
          cd ../../
          pwd
          git clone https://$GOTAUTH@github.com/brokenjade3000/got_auto.git
          cd got_auto
          chmod 775 ./script/install.sh
          ./script/install.sh
          
      # Runs the Super-Linter action
      - name: run_910
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
        run: |
          pwd
          cd /home/runner/work/got_auto/src
          python --version
          python run_daily_hof.py 9,10,11,12          
          
          
          
  run_1314:
    # Name the Job
    name: run_1314
    # Set the type of machine to run on
    runs-on: ubuntu-18.04

    steps:
      # Checks out a copy of your repository on the ubuntu-latest machine
      - name: Checkout code
        uses: actions/checkout@v2

      - name: set up python 3.6
        uses: actions/setup-python@v2
        with:
          python-version: 3.6

      - name: install packages
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
          GOTAUTH: ${{ secrets.GOTAUTH}}
        run: |
          cd ../../
          pwd
          git clone https://$GOTAUTH@github.com/brokenjade3000/got_auto.git
          cd got_auto
          chmod 775 ./script/install.sh
          ./script/install.sh
          
      # Runs the Super-Linter action
      - name: run_1314
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
        run: |
          pwd
          cd /home/runner/work/got_auto/src
          python --version
          python run_daily_hof.py 13,14,15,16           
          
          

  run_1718:
    # Name the Job
    name: run_1718
    # Set the type of machine to run on
    runs-on: ubuntu-18.04

    steps:
      # Checks out a copy of your repository on the ubuntu-latest machine
      - name: Checkout code
        uses: actions/checkout@v2

      - name: set up python 3.6
        uses: actions/setup-python@v2
        with:
          python-version: 3.6

      - name: install packages
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
          GOTAUTH: ${{ secrets.GOTAUTH}}
        run: |
          cd ../../
          pwd
          git clone https://$GOTAUTH@github.com/brokenjade3000/got_auto.git
          cd got_auto
          chmod 775 ./script/install.sh
          ./script/install.sh
          
      # Runs the Super-Linter action
      - name: run_1718
        env:
          DEFAULT_BRANCH: main
          MASTER_KEY: ${{ secrets.MASTER_KEY}}
        run: |
          pwd
          cd /home/runner/work/got_auto/src
          python --version
          python run_daily_hof.py 17,18,19,20
                   
